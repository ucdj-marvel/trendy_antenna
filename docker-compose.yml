version: "3"
services:
  postgres:
    build: .
    restart: always
    ports:
      - "5433:5432"
    volumes:
      - postgres_volume:/var/lib/postgresql/data
    env_file:
      - config/.env

volumes:
  postgres_volume: